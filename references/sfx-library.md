# SFX Library · huashu-design

> All generated by ElevenLabs Sound Generation API, Apple-keynote-grade audio quality.
> Production-grade SFX asset library covering all scenarios for huashu animations, demos, and product demos.

**Asset location**: `assets/sfx/<category>/<name>.mp3`
**Total count**: 37 SFX (30 batch-generated + 7 retained from v7b)
**Generation model**: ElevenLabs Sound Generation API (prompt_influence 0.4)
**Audio quality**: 44.1kHz MP3, Apple-keynote-grade clarity, no extra reverb

---

## Directory Structure

```
assets/sfx/
├── keyboard/      type, type-fast, delete-key, space-tap, enter
├── ui/            click, click-soft, focus, hover-subtle, tap-finger, toggle-on
├── transition/    whoosh, whoosh-fast, swipe-horizontal, slide-in, dissolve
├── container/     card-snap, card-flip, stack-collapse, modal-open
├── feedback/      success-chime, error-tone, notification-pop, achievement
├── progress/      loading-tick, complete-done, generate-start
├── impact/        logo-reveal, logo-reveal-v2, brand-stamp, drop-thud
├── magic/         sparkle, ai-process, transform
└── terminal/      command-execute, output-appear, cursor-blink
```

---

## Quick Index

### ⌨️ Keyboard (Keyboard Input)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/keyboard/type.mp3` | 0.5s | Single key press (mechanical keyboard single key) | mechanical keyboard single key press |
| `sfx/keyboard/type-fast.mp3` | 1.5s | Continuous fast typing (demo typing a prompt) | fast continuous typing rhythm, apple magic keyboard |
| `sfx/keyboard/delete-key.mp3` | 0.5s | Backspace / delete | single backspace key, low pitched thud |
| `sfx/keyboard/space-tap.mp3` | 0.5s | Light spacebar tap | soft spacebar tap, wide flat |
| `sfx/keyboard/enter.mp3` | 0.5s | Enter / confirm (v7b retained) | enter key press, crisp tactile |

### 🎯 UI (Interface Interactions)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/ui/click.mp3` | 0.5s | Standard UI click (v7b retained) | crisp modern interface click |
| `sfx/ui/click-soft.mp3` | 0.5s | Soft UI click (secondary buttons/links) | soft gentle button click, mid pitched |
| `sfx/ui/focus.mp3` | 0.5s | Element focus/select (v7b retained) | subtle focus tone, element highlight |
| `sfx/ui/hover-subtle.mp3` | 0.5s | Hover hint (microsecond-level feedback) | barely audible tick, air whisper |
| `sfx/ui/tap-finger.mp3` | 0.5s | Mobile tap (iOS interface) | finger tap on touchscreen, muted thud |
| `sfx/ui/toggle-on.mp3` | 0.5s | Toggle switch on | ios toggle switch flip, satisfying click |

### 🌊 Transition (Transitions)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/transition/whoosh.mp3` | 0.5s | Standard whoosh (v7b retained) | air whoosh transition |
| `sfx/transition/whoosh-fast.mp3` | 0.6s | Fast whoosh (title flash-in, tab switch) | quick fast air whoosh, cinematic |
| `sfx/transition/swipe-horizontal.mp3` | 0.7s | Horizontal swipe (carousel, tab switch) | smooth left-to-right air movement |
| `sfx/transition/slide-in.mp3` | 0.6s | Element slide in (side panel, drawer) | smooth soft whoosh with arrival |
| `sfx/transition/dissolve.mp3` | 0.8s | Soft dissolve (image fade out/in) | soft dissolve, airy shimmer |

### 🃏 Container (Cards / Containers)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/container/card-snap.mp3` | 0.5s | Card snap into place (v7b retained) | card snap into place |
| `sfx/container/card-flip.mp3` | 0.7s | Card flip (front/back switch) | playing card flip, crisp snap |
| `sfx/container/stack-collapse.mp3` | 0.8s | Stack collapse (list aggregation) | cards stacking, paper taps collapsing |
| `sfx/container/modal-open.mp3` | 0.6s | Modal open | modal popping open, whoosh + thud |

### 🔔 Feedback (Notifications / Feedback)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/feedback/success-chime.mp3` | 1.0s | Success notification (payment success, task complete) | two ascending bell tones, ios-style |
| `sfx/feedback/error-tone.mp3` | 0.7s | Error notification (warning, failure) | descending two-note warning, soft |
| `sfx/feedback/notification-pop.mp3` | 0.6s | Message pop-up (toast, notification) | notification bloop, ios message alert |
| `sfx/feedback/achievement.mp3` | 1.5s | Achievement unlocked (milestone, badge) | triumphant rising arpeggio, game-style |

### ⏳ Progress (Progress / Status)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/progress/loading-tick.mp3` | 0.5s | Loading timer (progress bar beat) | soft short pulse, minimal ambient |
| `sfx/progress/complete-done.mp3` | 0.8s | Completion confirmation (step done) | two ascending satisfying tones |
| `sfx/progress/generate-start.mp3` | 0.8s | AI starts generating | soft rising shimmer, magical whoosh |

### 💥 Impact (Brand / Impact)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/impact/logo-reveal.mp3` | 0.7s | Logo impact (v7b retained) | logo reveal thud |
| `sfx/impact/logo-reveal-v2.mp3` | 1.5s | Longer Logo impact (cinematic) | cinematic bass hit with shimmer tail |
| `sfx/impact/brand-stamp.mp3` | 1.0s | Heavy stamp (certification, seal) | rubber stamp thud, paper contact |
| `sfx/impact/drop-thud.mp3` | 0.7s | Object landing (insert, place) | heavy thud, wood surface contact |

### ✨ Magic (AI Transformation)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/magic/sparkle.mp3` | 0.8s | Magic flash (AI highlight, surprise) | bright twinkling stars, fairy dust |
| `sfx/magic/ai-process.mp3` | 1.2s | AI processing sound (thinking state) | modulating digital hum with shimmer |
| `sfx/magic/transform.mp3` | 1.0s | Transformation transition (morph effect) | rising shimmer whoosh with sparkle tail |

### 💻 Terminal (Command Line)

| File | Duration | Use | Prompt Key Points |
|---|---|---|---|
| `sfx/terminal/command-execute.mp3` | 0.5s | Command execution | crisp digital beep with tick, hacker ui |
| `sfx/terminal/output-appear.mp3` | 0.6s | Output appears | rapid digital ticks, retro printout |
| `sfx/terminal/cursor-blink.mp3` | 0.5s | Cursor blink | subtle soft digital pulse, rhythmic |

---

## Recommended Pairings by Scenario

### 💻 Terminal Interaction Demo
```
type (0.5s) → enter (0.5s) → command-execute (0.5s) → output-appear (0.6s)
```
Looping element: `cursor-blink` as ambient idle sound.

### 🃏 Card Selection Flow
```
hover-subtle (0.5s, UI hover) → click-soft (0.5s, click) → card-snap (0.5s, snap into place)
```
Or advanced version: `card-flip` for front/back switch.

### 🤖 AI Generation Full Flow
```
generate-start (0.8s, launch) → ai-process (1.2s, processing) → sparkle (0.8s, flash) → complete-done (0.8s, finish)
```
On error: replace `complete-done` with `error-tone`.

### 🎬 Logo Reveal (Brand Moment)
```
whoosh-fast (0.6s, buildup) → logo-reveal-v2 (1.5s, landing) → sparkle (0.8s, tail)
```
Simple version: `whoosh → logo-reveal` (v7b two-piece set directly).

### 📱 UI Interaction Demo (Mobile)
```
tap-finger (0.5s, tap) → slide-in (0.6s, panel slides in) → toggle-on (0.5s, toggle)
```
On completion: `success-chime` or `notification-pop`.

### 📊 Data Visualization / Dashboard
```
loading-tick (0.5s, beat) × N → complete-done (0.8s, data arrives) → achievement (1.5s, wow landing)
```

### 🎯 Form Submission Flow
```
click-soft (0.5s) → loading-tick ×2 (1.0s) → success-chime (1.0s)
```
Failure branch: `error-tone (0.7s)`.

### 🪄 Magic Transform Scene
```
whoosh-fast (0.6s) → transform (1.0s) → sparkle (0.8s)
```
Good for: element morphing, before/after comparisons, "AI rewrite" demos.

---

## Usage Guidelines

### Volume Recommendations (from apple-gallery-showcase.md dual-track audio)
- **SFX main track**: `1.0` (no attenuation)
- **BGM background track**: `0.4 ~ 0.5` (SFX clearly cuts through)
- **Multiple SFX layered**: use `amix=inputs=N:duration=longest:normalize=0` to preserve dynamic range

### ffmpeg Concatenation Templates
```bash
# Single SFX aligned to a time point:
ffmpeg -i video.mp4 -itsoffset 2.5 -i sfx/ui/click.mp3 \
  -filter_complex "[0:a][1:a]amix=inputs=2:duration=longest:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4

# Multiple SFX + BGM:
ffmpeg -i video.mp4 \
  -itsoffset 1.0 -i sfx/transition/whoosh-fast.mp3 \
  -itsoffset 1.6 -i sfx/impact/logo-reveal-v2.mp3 \
  -i bgm.mp3 \
  -filter_complex "[3:a]volume=0.4[bgm];[0:a][1:a][2:a][bgm]amix=inputs=4:normalize=0[a]" \
  -map 0:v -map "[a]" output.mp4
```

### Selection Decision Tree
1. **Tactile action** (typing / clicking / swiping) → `keyboard/` or `ui/`
2. **Element enters/exits** → `transition/`
3. **Container-level operation** (card / modal) → `container/`
4. **Status feedback** (success / failure / notification) → `feedback/`
5. **Progress / time passing** → `progress/`
6. **Brand landing / important moment** → `impact/`
7. **AI magic / transformation** → `magic/`
8. **Command line / code demo** → `terminal/`

### Avoid Stacking Too Many Sounds
- Maximum **2 SFX simultaneously** at any time point
- If BGM is below 0.3, can allow 3
- Clear all other SFX at brand impact moment (leave 0.2s of silence before the landing)

---

## Prompt Writing Principles (For Reuse)

Reference style: `apple keynote, tight, minimal, no reverb unless ambient, crisp, elegant`

**Three elements of a good prompt**:
1. **Physical sound description**: what object, what action ("mechanical keyboard single key press")
2. **Texture/style specification**: apple-style / ios-style / cinematic / retro
3. **Negative exclusion**: no reverb / clean studio / minimal

❌ "click sound"
✅ "crisp ui button click, clean modern interface sound, apple-style, high pitched"

❌ "magic"
✅ "bright twinkling stars sound, high pitched glittery chime, fairy dust"

---

## See Also
- Dual-track audio and ffmpeg concatenation: `apple-gallery-showcase.md`
- Original generation script: `/tmp/gen_sfx_batch.sh` (one-time batch generator)
